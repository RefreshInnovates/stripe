{
  "name": "stripe-backend",
  "version": "1.0.0",
  "description": "A backend for Stripe Terminal connection tokens",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "stripe": "^8.0.0"
  }
}
